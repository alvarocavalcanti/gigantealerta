<style type="text/css">
  #menu {
    position: absolute;
    width: 100%;
    left: 0px;
    bottom: 0px;
  }

  #menu-toggle {
    position: relative;
    width: 50px;
    z-index: 3;
  }

  #menu-items {
    position: relative;
    z-index: 4;
    width: 100%;
  }

  .expanded {
  }

  .collapsed {
  }
</style>

<div id="menu" class="text-center collapsed">
  <button type="button" id="menu-toggle" class="btn btn-default btn-lg">
   <span id="menu-handle-icon" class="glyphicon glyphicon-chevron-up"></span>
  </button>
  <div class="text-center" style="width: 100%">
    <div id="menu-items" class="text-center">
      <a href="#" id="btn-encontro"><img src="/assets/meetingPoint.png" alt="Encontro"></a>
      <a href="#" id="btn-conflito"><img src="/assets/conflict.png" alt="Conflito"></a><br>
      <a href="#" id="btn-policia"><img src="/assets/policeLine.png" alt="Polícia"></a>
      <a href="#" id="btn-depedracao"><img src="/assets/depredation.png" alt="Depedração"></a>
    </div>
  </div>
</div>
<div id="loading" class="loading">Loading Map...</div>
<div id="map_canvas" class="map"/>

<script type="text/javascript">

  // -- Declarations
  
  // -- Static calls
  //arrangeMenuItems();
  $('#menu-items').hide();

  // -- Declarations
  function expandMenu() {
    $('#menu').removeClass('collapsed');
    $('#menu').addClass('expanded');

    $('#menu-handle-icon').removeClass('glyphicon-chevron-up');
    $('#menu-handle-icon').addClass('glyphicon-chevron-down');

    $('#menu-items').show('fast');
  }

  function collapseMenu() {
    $('#menu').removeClass('expanded');
    $('#menu').addClass('collapsed');

    $('#menu-handle-icon').addClass('glyphicon-chevron-up');
    $('#menu-handle-icon').removeClass('glyphicon-chevron-down');

    $('#menu-items').hide('fast');
  }

  function buttonClicked(btnType) {

  }

  function arrangeMenuItems() {
    var togglePos = $('#menu-toggle').position();

    $('#btn-encontro').css({
      position: 'absolute',
      top: togglePos.top - $('#btn-encontro').outerHeight() + 'px',
      left: togglePos.left - $('#btn-encontro').outerWidth() + 'px'
    });
  }
  
  // -- Callbacks
  $('#menu-toggle').on({
    click: function () {
      if ($('#menu').hasClass('collapsed')) {
        expandMenu();
      } else {
        collapseMenu();
      }
    }
  });

  $('#btn-depedracao').on({
    click: function () {
      buttonClicked('Depedração');
    }
  });

  $('#btn-policia').on({
    click: function () {
      buttonClicked('Polícia');
    }
  });

  $('#btn-encontro').on({
    click: function () {
      buttonClicked('Encontro');
    }
  });

  $('#btn-conflito').on({
    click: function () {
      buttonClicked('Conflito');
    }
  });
</script>